# Ansible vault password — keep a copy in your password manager.
# Without it, vault.yml and every backup are unrecoverable.
ansible/.vault-pass
*.retry
.venv/
history-exports/
history-export-manifest*.yml
*-element-keys.txt
*-passphrase.txt
