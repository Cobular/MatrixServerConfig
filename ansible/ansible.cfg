[defaults]
host_key_checking = False
result_format = yaml
retry_files_enabled = False
# Relative to cwd; ansible.cfg is only discovered when cwd is this directory,
# so this always resolves correctly.
vault_password_file = .vault-pass
pipelining = True

[privilege_escalation]
become = True
become_method = sudo
